<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cn.bingoogolapple.qrcode.zxingdemo">
    <!--
        为了方便写demo，引入了 https://github.com/bingoogolapple/BGAPhotoPicker-Android
        和 https://github.com/bingoogolapple/BGAAdapter-Android 这两个库
        你自己项目里没用这两个库的话不需要加这个
    -->
    <uses-sdk tools:overrideLibrary="cn.bingoogolapple.photopicker,cn.bingoogolapple.androidcommon.adapter" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TestScanActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TestGeneratectivity"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CurMapActivity"
            android:label="@string/title_activity_cur_map" />
        <!-- <activity android:name=".LoginActivity" /> -->
        <activity android:name=".ScanActivity" />
        <activity android:name=".SuccessScanActivity" />
        <activity android:name=".PhotoActivity" />
        <activity android:name=".AdjustLocationActivity" />
        <activity android:name=".ConfirmPhotoActivity" />
        <activity android:name=".ConfirmUploadActivity" />
        <activity android:name=".UploadResultActivity" />
        <activity android:name=".FailScanActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LocalDataActivity" />
        <activity android:name=".NavigationMenuActivity" />
        <activity android:name=".MaintenanceActivity" />
        <activity android:name=".MaintenanceInfoActivity" />
        <activity android:name=".ReportRepairActivity" />
        <activity android:name=".ScanRepairActivity" />
        <activity android:name=".MaintenancePhotoActivity" />
        <activity android:name=".MaintenanceLightActivity" />
        <activity android:name=".SuccessReportActivity"></activity>
    </application>

</manifest>